# Security Policy (org default)

Report security vulnerabilities in any `pharm0-ai` repository to **security@pharm0.ai**. Abuse reports to **abuse@pharm0.ai** (RFC 2142).

Per-repo policies override this default; see the repo's own `SECURITY.md` for scope.

We follow a 90-day coordinated-disclosure window by default, negotiable for severity.
